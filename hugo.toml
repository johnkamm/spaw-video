baseURL = 'https://spstrat.com/'
languageCode = 'en-us'
title = 'Southpaw Strategies'

disableKinds = ["taxonomy", "term"]

canonifyURLs = true
relativeURLs = false
publishDir = "public"
assetDir = "assets"
staticDir = ["static"]

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true